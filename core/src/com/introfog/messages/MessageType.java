package com.introfog.messages;

public enum MessageType{
	move, deleteObject, characterChange, pushOut, characterSelected, levelComplete, getStartPosition,
	returnStartPosition, destroyObject, playerLost, addObject, goTo, comeTo, changeState,
	disconnect, unknown
}
